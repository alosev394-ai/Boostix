# Матрица тестирования Boostix

Этот документ определяет минимальный объём проверки перед RC и stable. Строка
считается пройденной только при наличии ссылки на CI, журнала или ручного
протокола с датой, версией и исполнителем.

## Автоматический контур

Команда полного локального прогона:

```powershell
powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass `
  -File .\tests\Run-All.ps1 `
  -ResultsPath "$env:TEMP\boostix-test-results.json"
```

`Run-All.ps1`:

- требует 64-битный Windows PowerShell 5.1;
- проверяет наличие обязательного базового набора тестов;
- автоматически запускает каждый новый `Test-*.ps1`;
- запускает тесты в отдельных процессах;
- ограничивает время каждого теста;
- завершает прогон ошибкой при пропуске, тайм-ауте или ненулевом exit code;
- создаёт машиночитаемый JSON-отчёт при переданном `ResultsPath`.

GitHub Actions должен начинать с удаления готовых `work` и `dist`, собирать
Release из исходников и только затем запускать полный набор.

## Поддерживаемые конфигурации Windows

| ID | Конфигурация | RC | Stable | Результат/ссылка |
| --- | --- | --- | --- | --- |
| OS-01 | Windows 10 x64, последняя заявленная сборка | Обязательно, если поддерживается | Обязательно | |
| OS-02 | Windows 11 x64, предыдущая поддерживаемая сборка | Обязательно | Обязательно | |
| OS-03 | Windows 11 x64, текущая поддерживаемая сборка | Обязательно | Обязательно | |
| OS-04 | Чистая локальная учётная запись администратора | Обязательно | Обязательно | |
| OS-05 | Обычный пользователь + UAC | Обязательно | Обязательно | |
| OS-06 | UAC с учётными данными другого администратора | Обязательно | Обязательно | |
| OS-07 | Имя пользователя на кириллице/Unicode | Обязательно | Обязательно | |
| OS-08 | Установка не на системный диск | Обязательно | Обязательно | |
| OS-09 | Длинный путь и папка с пробелами | Обязательно | Обязательно | |

Для каждой версии Windows сохраняются номер сборки, перечень обновлений,
архитектура, состояние VBS/Memory Integrity и режим UAC.

## Оборудование

| ID | Конфигурация | Минимум |
| --- | --- | --- |
| HW-01 | Intel desktop CPU | 1 ПК |
| HW-02 | Intel hybrid P/E-core CPU | 1 ПК |
| HW-03 | AMD Ryzen | 1 ПК |
| HW-04 | AMD X3D | 1 ПК |
| HW-05 | NVIDIA GPU | 2 поколения |
| HW-06 | AMD GPU | 1 ПК |
| HW-07 | Intel Arc | 1 ПК |
| HW-08 | iGPU + дискретная GPU | 1 ноутбук |
| HW-09 | 8/16/32/64 ГБ RAM | По 1 конфигурации |
| HW-10 | Pagefile auto/fixed/disabled | Все три режима |
| HW-11 | Один монитор | 1 ПК |
| HW-12 | Два монитора с разным DPI | 1 ПК |
| HW-13 | Ноутбук: сеть/батарея/sleep-resume | Все режимы |

## Разрешение, DPI и доступность

| ID | Сценарий | Критерий |
| --- | --- | --- |
| UI-01 | 1366×768, 100% | Все функции доступны, нет обрезания |
| UI-02 | 1920×1080, 100/125/150% | Текст резкий, отступы и центровка стабильны |
| UI-03 | 2560×1440, 100/150% | Нет размытия или скачка размеров |
| UI-04 | 3840×2160, 150/200% | Нет обрезания и микроскопического текста |
| UI-05 | Перенос между мониторами с разным DPI | Окно пересчитывается без перезапуска |
| UI-06 | Увеличение текста Windows 200% | Контент прокручивается и остаётся доступным |
| UI-07 | Только клавиатура | Логичный Tab-порядок, Enter/Space/Escape работают |
| UI-08 | Narrator | Интерактивные элементы имеют понятные имена |
| UI-09 | High Contrast | Состояния различимы без одного только цвета |
| UI-10 | Reduced Motion | Нет обязательной интенсивной анимации |
| UI-11 | Контраст | Обычный текст ≥ 4.5:1, крупный ≥ 3:1 |
| UI-12 | RDP | Окно работает и остаётся резким |

Снимки для UI-01—UI-05 делаются при одинаковом состоянии экрана и прикладываются
к протоколу для визуального сравнения.

## Установка, обновление и восстановление

Каждый сценарий выполняется на чистом snapshot Windows и заканчивается проверкой
файлов, реестра, ярлыков, задач, служб и локальных данных.

| ID | Сценарий | Ожидаемый результат |
| --- | --- | --- |
| LC-01 | Чистая установка | Все компоненты проверены, приложение запускается |
| LC-02 | Отказ в UAC | Нет частично установленного состояния |
| LC-03 | Повторная установка той же версии | Идемпотентный результат |
| LC-04 | Обновление с каждой поддерживаемой версии | Настройки и данные сохранены |
| LC-05 | Обрыв скачивания | Временный файл удалён, текущая версия цела |
| LC-06 | Неверная подпись/хеш/размер/версия | Установка заблокирована |
| LC-07 | Завершение installer на каждой фазе | Автоматический recovery/rollback |
| LC-08 | Перезагрузка во время транзакции | Восстановление при следующем запуске |
| LC-09 | Недостаточно места | Безопасный отказ до необратимого изменения |
| LC-10 | Файл заблокирован | Понятная ошибка и рабочий rollback |
| LC-11 | Junction/symlink в управляемом каталоге | Операция отклонена |
| LC-12 | Удаление без активной оптимизации | Полное удаление разрешённых артефактов |
| LC-13 | Удаление при незавершённом восстановлении | Удаление безопасно заблокировано |
| LC-14 | Неверный/повреждённый backup | Изменения не применяются вслепую |

## Системные изменения

Для каждого отдельного компонента оптимизации нужны четыре теста:

1. исходное значение уже совпадает с целевым;
2. исходное значение отличается;
3. пользователь меняет значение после Boost;
4. применение прерывается между записью состояния и мутацией.

Критерии:

- неподдерживаемая настройка пропускается с понятным статусом;
- уже целевое значение не перезаписывается без необходимости;
- восстановление возвращает точное исходное значение;
- более позднее изменение пользователя не затирается;
- сбой одного необязательного компонента не повреждает остальные;
- ни один путь не выходит за allowlist;
- все reparse point отклоняются до записи или удаления.

## Память и производительность

| ID | Сценарий | Критерий |
| --- | --- | --- |
| PF-01 | Нет давления памяти | Очистка не запускается без причины |
| PF-02 | Краткий единичный всплеск | Нет агрессивной реакции |
| PF-03 | Подтверждённое давление | Срабатывает только безопасное обслуживание |
| PF-04 | Pagefile выключен/мал | Диагностика указывает commit-риск |
| PF-05 | VRAM заполнена | Не выдаётся ложное обещание очистить RAM |
| PF-06 | 8-часовая сессия | Нет значимого роста памяти, handles и timers |
| PF-07 | 100 включений/выключений Boost | Нет зависших процессов и утечки состояния |
| PF-08 | Измерение FPS отменено | Нет оставшегося capture-процесса или файла |
| PF-09 | Два одинаковых A/B прогона | Метрики воспроизводимы в заданном допуске |

Boostix не должен очищать standby list Windows, уменьшать working set целевого
приложения или заявлять освобождение памяти, которого не было.

## Сеть

- полностью offline;
- DNS failure;
- TLS error;
- HTTP 403, 404, 429 и 5xx;
- медленный ответ и slow-drip download;
- VPN и proxy;
- корпоративная TLS inspection;
- обрыв соединения на 0%, 50% и 99%;
- повтор после временной ошибки;
- недоступен основной endpoint, доступен разрешённый fallback.

Ни одна сетевая ошибка не должна повреждать установленную версию. Ошибка
криптографической проверки никогда не должна переходить к установке.

## Stable evidence

Перед stable должны быть подтверждены:

- 0 открытых P0/P1, связанных с безопасностью или потерей данных;
- 100 последовательных циклов apply → reboot → restore без необратимых изменений;
- не менее 20 разных физических ПК;
- не менее 500 часов суммарных сессий;
- не менее 100 полных Boost-сессий;
- три подряд зелёных CI-прогона из чистого checkout;
- успешность установки и обновления не ниже 99%;
- crash-free sessions не ниже 99%;
- 30 дней без нового критического дефекта.

