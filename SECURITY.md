# Безопасность Boostix

Boostix изменяет параметры производительности Windows только после явного
действия пользователя. Установка, восстановление системных настроек и другие
операции, требующие повышенных прав, должны сопровождаться стандартным запросом
UAC. Приложение не должно отключать антивирус, брандмауэр, проверку подписей или
другие защитные механизмы Windows.

## Поддерживаемые версии

Стабильная версия Boostix пока не опубликована. Текущая предварительная версия —
**2.0.0 BETA** — поддерживается только в период активного тестирования. После
появления stable исправления безопасности будут выпускаться для последней
поддерживаемой стабильной версии. Старые beta-сборки следует обновлять перед
дальнейшим использованием.

| Канал | Поддержка безопасности |
| --- | --- |
| Последняя стабильная версия | Ещё не опубликована |
| Текущая beta/RC | Да, в период тестирования |
| Более старые версии | Нет |

## Как сообщить об уязвимости

Не публикуйте сведения об уязвимости в обычном Issue, обсуждении или чате.
Используйте приватный Security Advisory в GitHub-репозитории:

1. Откройте вкладку **Security** репозитория.
2. Выберите **Report a vulnerability**.
3. Укажите версию Boostix и версию Windows.
4. Приложите минимальные шаги воспроизведения, ожидаемый и фактический результат.
5. Опишите возможное влияние и требуются ли права администратора.
6. Добавьте обезличенный диагностический отчёт, если он действительно нужен.

Не прикладывайте пароли, токены, приватные ключи, полные дампы памяти или
необработанные пользовательские документы.

Целевой срок первичного ответа — семь календарных дней. Срок исправления зависит
от серьёзности и воспроизводимости проблемы. До выпуска исправления просим не
раскрывать технические детали публично. Программа bug bounty не заявлена.

## Особо важные классы проблем

- обход UAC или выполнение команд без явного согласия;
- выход путей установки, обновления, восстановления или удаления за разрешённые
  каталоги;
- обработка junction, symlink или других reparse point как обычной папки;
- установка файла с неверной подписью, версией, размером или SHA-256;
- возможность подменить резервную копию системных настроек;
- удаление или перезапись пользовательских файлов;
- выполнение команд, полученных по сети;
- утечка персональных путей или иных чувствительных данных через экспорт.

## Модель доверия

- Elevated installer до инициализации файлового журнала и WinForms исключает
  текущий каталог из нативного DLL search и разрешает загрузку системных DLL
  только из System32. Если безопасную политику применить нельзя, установщик
  завершается до основной операции.
- Проверка обновлений выполняется по HTTPS только через allowlist-адреса.
  Manifest/signature могут загружаться через GitHub или jsDelivr; EXE — только
  через GitHub raw и GitHub Releases. jsDelivr не используется как источник
  исполняемого файла. Содержимое ответа не может добавить произвольный источник
  или сетевую команду.
- Манифест обновления должен иметь корректную RSA-подпись Boostix. Эта подпись
  подтверждает целостность и авторство метаданных канала, но не является
  Authenticode-подписью EXE и не гарантирует независимую доступность или
  свежесть CDN-зеркала.
- Установщик должен совпадать с подписанным манифестом по версии, размеру,
  SHA-256 и разрешённому `ProductName`.
- Временный пакет обновления хранится в отдельном каталоге с защищённым ACL для
  текущего пользователя, Administrators и SYSTEM. Reparse point и выход за
  разрешённый каталог блокируются.
- Проверка свободного места на томе `%TEMP%` является предварительной и
  best-effort. Redirected, quota-backed и сетевой путь может не предоставлять
  достоверный `DriveInfo`; окончательной проверкой служат защищённое создание
  каталога и запись в уже открытый удерживаемый файл.
- Повышенные права запрашиваются только для заранее определённых локальных
  действий.
- Сетевая ошибка, локальная ошибка записи и ошибка целостности обрабатываются
  отдельно. Ошибка подписи, размера, SHA-256, версии или PE-структуры не должна
  превращаться в повторное исполнение либо в обычное сетевое предупреждение.
- Повреждённый предыдущий EXE или файл с неожиданной идентичностью продукта не
  должен запускаться как цель отката. Repair выполняется транзакционно без
  исполнения такого файла.
- Каждый elevated-запуск использует уникальный
  `%ProgramData%\Boostix\Logs\setup-<correlation>.log` в каталоге с защищённым
  ACL и без reparse point. Пользовательский `%TEMP%` не используется как
  fallback. Журнал содержит нормализованный режим и число аргументов, но не их
  сырые значения.
- Elevated health probe не читает пользовательские preferences/legacy markers и
  не пишет в LocalAppData. Проверка готовности использует только in-memory
  компоненты, создаёт аутентифицированный одноразовый сигнал в защищённой
  транзакции, а итог фиксирует родительский установщик в setup-журнале.
- Каталог с невалидным именем внутри защищённого `UpdateRollback` не считается
  транзакцией, не удаляется и не блокирует остальные установки. Валидно
  названное, но повреждённое состояние обрабатывается fail-closed, чтобы не
  уничтожить возможную резервную копию.
- Ярлыки создаются нативным Windows Shell Link API, без запуска сценариев через
  Windows Script Host.
- Резервные копии системных изменений должны проверяться до восстановления.
- Приложение не должно внедряться в процессы, изменять исполняемые файлы целевых
  программ или обходить механизмы защиты.
- Диагностика хранится локально и передаётся только по инициативе пользователя.
- Идентичность игровой цели закрепляется по PID, времени запуска и полному пути;
  PID без совпадения остальных признаков не считается доверенной целью.
- Evidence-only Crash Assistant ограничивает поиск событий временем и
  идентичностью цели. Корреляция WER/Application Error не объявляется доказанной
  причиной сбоя.
- Сессионный план питания применяется только от сети и восстанавливается только
  при совпадении защищённого маркера; внешнее переключение пользователя не
  перезаписывается.
- Background Impact не завершает процессы принудительно. В Boostix нет очистки
  standby list, working-set trim цели и принудительного GC.

## Требования к релизу

Stable-релиз нельзя публиковать, если:

- обязательный CI не прошёл полностью;
- хотя бы один `Test-*.ps1` был пропущен;
- подпись или контрольная сумма артефакта не подтверждена;
- EXE не имеет действительной Authenticode-подписи доверенного издателя с RFC
  3161 timestamp;
- не проверены установка, обновление, откат и удаление на чистой Windows;
- открыт критический дефект, связанный с правами, путями, обновлением,
  восстановлением или пользовательскими данными.

Подробные условия находятся в
[`docs/RELEASE-CHECKLIST.md`](docs/RELEASE-CHECKLIST.md).

## Известное ограничение подписи 2.0.0 BETA

Исполняемые файлы 2.0.0 BETA пока не подписаны доверенным
Authenticode-сертификатом. Windows может показывать неизвестного издателя, а
SmartScreen, WDAC или корпоративная политика — блокировать запуск. Подписанный
RSA-манифест и SHA-256 предотвращают незаметную подмену пакета в канале
обновлений, но не подтверждают издателя файла для Windows.

Это блокер stable-релиза, а не повод отключать антивирус, SmartScreen, UAC или
корпоративные правила. Универсальная совместимость установки до получения
сертификата и завершения E2E-матрицы Windows 10/11 не заявляется.

## Область, не являющаяся уязвимостью

Разница FPS между компьютерами, отсутствие ожидаемого прироста, высокая
загрузка памяти целевой программой и предупреждение SmartScreen без возможности
обхода защиты сами по себе не являются уязвимостью. Их всё равно можно сообщать
как обычные ошибки или проблемы совместимости.
